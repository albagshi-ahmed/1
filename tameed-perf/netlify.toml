[build]
  functions = "netlify/functions"

# Hourly refresh of the Google channel only.
[functions."refresh-google"]
  schedule = "@hourly"

# --- ENABLE ONLY AFTER SEEDING (run order step 7) ---
# Serve /tameed-live.json straight from the Netlify Blobs "tameed" store via
# the serve-google function. Keep COMMENTED until seed-once + the first
# refresh-google have populated the store, otherwise the path 404s.
#
# [[redirects]]
#   from = "/tameed-live.json"
#   to = "/.netlify/functions/serve-google"
#   status = 200
#   force = true
